# doc/magenetic_flux_integral/scripts/ — 磁通積分 LaTeX 原稿

**用途**：存放磁通積分網格相關推導的 **LaTeX 原稿（.tex）**。
**內容**：`mesh_size.tex`（網格數量 N=81×81 與網格間距）、`grid_value.tex`（格點取值方式）。
**命名 / 慣例**：葉夾 = .tex 原稿；編譯 PDF 放隔壁 ../pdf/。資料夾名拼字 `magenetic` 為既有命名（勿改）。
**相關**：PDF 見 ../pdf/；見 ../README.md。
