# doc/magenetic_flux_integral/pdf/ — 磁通積分編譯 PDF

**用途**：存放磁通積分 .tex 的**編譯 PDF**。
**內容**：`mesh_size.pdf`（網格數量與間距）、`grid_value.pdf`（格點取值）。
**命名 / 慣例**：葉夾 = 編譯 PDF；與 ../scripts/ 同名 .tex 對應。資料夾名拼字 `magenetic` 為既有命名（勿改）。
**相關**：原稿見 ../scripts/；見 ../README.md。
