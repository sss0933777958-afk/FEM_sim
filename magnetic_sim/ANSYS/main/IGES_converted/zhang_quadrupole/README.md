# IGES_converted/zhang_quadrupole/ — Zhang Quadrupole 單位轉換後 IGES（給 IGESIN）

**用途**：`../../IGES/zhang_quadrupole/` 經單位/旗標轉換後、可被 ANSYS `IGESIN` 正確讀入的 Zhang 4-pole Quadrupole 幾何。

**內容**：`Zhang_Quadrupole_Lp0p405.iges`（目前唯一一份，與 `IGES/` 端對應）。

**資料來源 / 流向**：由 `../../IGES/zhang_quadrupole/` 轉換而來 → `apdl/zhang_quadrupole/geom` 用 `IGESIN` → ANSYS。

**命名 / 慣例**：`Lp0p405` = L_P 0.405mm；須與 `IGES/` 端同步。MKS 轉換用 **flag 2 / 重 export**，不可抄 hung sed 6→1。

**相關**：`../README.md`、`../../IGES/zhang_quadrupole/`、`doc/workflows/iges-sync-quick.md`、`iges-model-id.md`。
