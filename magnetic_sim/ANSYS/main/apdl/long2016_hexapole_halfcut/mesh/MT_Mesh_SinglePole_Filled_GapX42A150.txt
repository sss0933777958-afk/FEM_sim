! ============================================================================
! MT_Mesh_SinglePole.txt — Long Fei SINGLE POLE (filled full cone) MESH-ONLY deck
!   1 lower pole (azimuth 0deg, half-cut BLOCK+VSBV SKIPPED -> FULL cone) + 4
!   support-base BLOCKs + 1 protrusion (coil pillar) + air domain
!   (R7 inner sphere @ pole tip + R80 x H70 outer cylinder).
!   UNIFORM iron mesh: ESIZE 0.3mm on all iron; air graded (inner 0.4mm / outer 4mm).
!   SOURC36 coil built (placeholder current; the SOLVE deck overwrites with 1A).
!   Materials assigned by location+size (VOVLAP renumbers -> no hardcoded volume IDs).
!   SAVE -> db/geom/singlepole/mesh_singlepole.db (NO /SOLU, NO MAG BC). MKS metre.
!   mesh/solve SEPARATED: solve deck MT_Sim_SinglePole.txt RESUMEs this .db.
! ============================================================================
/CWD,'G:\my_workspace\code\FEM_sim\magnetic_sim\ANSYS\main\ANSYS_data\long2016_hexapole_halfcut\db\geom\singlepole\filled_gap_x42a150'
Finish
/cle
/CLEAR,NOSTART
/TITLE, Long2016 single pole filled - long air domain + graded + solve
/FILNAME,mesh_filled,1
/PREP7

! ----- parameters (MKS metre, from baseline) -----
POLE_NUM = 6
*afun, DEG
BTOL, 1e-6
/UNITS, MKS

R_norm    = 500e-6
R_norm_xy = R_norm*sqrt(2.0/3.0)
R_norm_z  = R_norm/sqrt(3.0)

YOKE_IN_R  = 84e-3/2.0
YOKE_OUT_R = 106e-3/2.0
YOKE_MID_R = (YOKE_IN_R + YOKE_OUT_R)/2.0
YOKE_H     = 2.0e-3
PROT_R     = 10.0e-3/2.0
PROT_H     = 7.0e-3

POLE_R        = 6.0e-3/2
POLE_TIP_R    = 40.0e-6
POLE_CONE_LEN = 15e-3
POLE_CONE_HYP = sqrt(POLE_CONE_LEN**2 + POLE_R**2)
ANG1 = acos(POLE_R/POLE_CONE_HYP)
ANG2 = 90 - ANG1
L = POLE_CONE_LEN*POLE_TIP_R/POLE_R
POLE_TIP_CENTER = L/cos(ANG2)
POLE_CONE2_LEN  = L*cos(ANG2)
POLE_TIP_R2     = POLE_TIP_R*cos(ANG2)
POLE_TIP_FILLET_DROP = POLE_TIP_CENTER - POLE_TIP_R
POLE_TIP_FILLET_PROT = (POLE_CONE2_LEN - POLE_TIP_FILLET_DROP)

ESIZE_IRON  = 0.3e-3     ! [ADDED] uniform iron element size
ESIZE_SPH   = 0.4e-3     ! [ADDED] inner air sphere
ESIZE_OUTER = 4.0e-3     ! [ADDED] outer air cylinder

! ===== [MODIFIED] single lower protrusion (coil pillar) at azimuth 0deg =====
WPLANE, 1,0,0,0,,,,,,
WPcsys,-1,0
CYL4, YOKE_MID_R*cos(0), YOKE_MID_R*sin(0), PROT_R, ,,, -PROT_H

! ===== [MODIFIED] single lower pole (azimuth 0deg) — FILLED full cone =====
X0_LOW = YOKE_MID_R*cos(0)
Y0_LOW = YOKE_MID_R*sin(0)
Z0_LOW = -PROT_H

WPLANE, 1,0,0,0,,,,,,
WPcsys,-1,0
ROTA_ANG = 0
WPROTA, ROTA_ANG

BLOCK, X0_LOW - 5e-3,  X0_LOW + 11e-3, Y0_LOW - 5e-3,  Y0_LOW + 5e-3,  Z0_LOW - 10e-3, Z0_LOW
BLOCK, X0_LOW - 5e-3,  X0_LOW - 2e-3,  Y0_LOW - 10e-3, Y0_LOW + 10e-3, Z0_LOW - 10e-3, Z0_LOW - 5e-3
BLOCK, X0_LOW - 10e-3, X0_LOW - 2e-3,  Y0_LOW - 5e-3,  Y0_LOW + 5e-3,  Z0_LOW - 10e-3, Z0_LOW - 2e-3
BLOCK, X0_LOW - 10e-3, X0_LOW - 5e-3,  Y0_LOW - 10e-3, Y0_LOW + 10e-3, Z0_LOW - 10e-3, Z0_LOW - 5e-3

*get, KP_NUM, KP  , ,NUM, MAX
*get, LI_NUM, LINE, ,NUM, MAX
KP_START = KP_NUM + 1
LI_START = LI_NUM + 1
numstr, KP,   KP_START
numstr, LINE, LI_START

l1 = R_norm_xy
l1_ang = atan2( 0 , R_norm_xy )
l2 = sqrt( (R_norm_xy + POLE_TIP_FILLET_PROT)**2 + POLE_TIP_R2**2 )
l2_ang = atan2( POLE_TIP_R2 , (R_norm_xy + POLE_TIP_FILLET_PROT) )
POLE_CONE_LEN_SHIFT = POLE_CONE_LEN + (R_norm_xy - POLE_TIP_FILLET_DROP)
l3 = sqrt( POLE_CONE_LEN_SHIFT**2 + POLE_R**2 )
l3_ang = atan2( POLE_R , POLE_CONE_LEN )
l4 = sqrt( (X0_LOW - 10e-3)**2 + POLE_R**2 )
l4_ang = atan2( POLE_R , (X0_LOW - 10e-3) )
l5 = abs( X0_LOW - 10e-3 )
l5_ang = atan2( 0 , (X0_LOW - 10e-3) )
l6 = abs( R_norm_xy + POLE_TIP_R )
l6_ang = atan2( 0, R_norm_xy + POLE_TIP_R )

K , , l1*cos(ROTA_ANG + l1_ang) , l1*sin(ROTA_ANG + l1_ang), Z0_LOW - 6e-3
K , , l2*cos(ROTA_ANG + l2_ang) , l2*sin(ROTA_ANG + l2_ang), Z0_LOW - 6e-3
K , , l3*cos(ROTA_ANG + l3_ang) , l3*sin(ROTA_ANG + l3_ang), Z0_LOW - 6e-3
K , , l4*cos(ROTA_ANG + l4_ang) , l4*sin(ROTA_ANG + l4_ang), Z0_LOW - 6e-3
K , , l5*cos(ROTA_ANG + l5_ang) , l5*sin(ROTA_ANG + l5_ang), Z0_LOW - 6e-3
K , , l6*cos(ROTA_ANG + l6_ang) , l6*sin(ROTA_ANG + l6_ang), Z0_LOW - 6e-3

LARC , KP_START , KP_START + 1 , KP_START + 5 , POLE_TIP_R
L , KP_START + 1 , KP_START + 2,,,,,,,,
L , KP_START + 2 , KP_START + 3,,,,,,,,
L , KP_START + 3 , KP_START + 4,,,,,,,,
L , KP_START + 4 , KP_START,,,,,,,,

*get, AR_NUM, AREA, ,NUM, MAX
AR_START = AR_NUM + 1
numstr, AREA, AR_START

AL, LI_START, LI_START + 1, LI_START + 2, LI_START + 3, LI_START + 4
VROTAT, AR_START, ,,,,,KP_START,KP_START + 4,,4

! [MODIFIED] FILLED pole: half-cut BLOCK+VSBV OMITTED -> full cone retained.
ALLSEL


! ===== [ADDED] merge steel (protrusion+blocks+cone) -> 1 vol =====
ALLSEL, ALL
VADD, ALL
NUMCMP, VOLU
*get, vsteel, VOLU,, NUM, MAX
! ===== FILLED: no tip cut =====
! [ADDED-GAP] 200um base-slab air gap @ built-frame x=42.5e-3 (finite BLOCK VSBV,KEEP; base z[-17,-7] only)
!   VGEN 平移前切、slab 隨後被平移；protrusion(z>-7)/cone 不動。
WPLANE, 1,0,0,0,,,,,,
WPcsys,-1,0
GAP_X = 42.5e-3
! [ADDED-GAP-A150] FULL base cross-section (150mm2) = A(y±5,z[-17,-7]) VADD B(y±10,z[-17,-12])
BLOCK, GAP_X, GAP_X + 0.2e-3, -5e-3, 5e-3, -17e-3, -7e-3
*get, vgblk_A, VOLU,,NUM,MAX
BLOCK, GAP_X, GAP_X + 0.2e-3, -10e-3, 10e-3, -17e-3, -12e-3
*get, vgblk_B, VOLU,,NUM,MAX
VADD, vgblk_A, vgblk_B
*get, vgblk, VOLU,,NUM,MAX
VSBV, vsteel, vgblk, , , KEEP
ALLSEL, ALL
NUMCMP, VOLU

! ===== [MODIFIED] re-orient pole to +x axis: tip apex -> (R_norm,0,0), WP at ORIGIN =====
!   cone axis already +x -> pure translation (no rotation). tip 500um toward origin, body +x.
X_SHIFT = R_norm - R_norm_xy
Z_SHIFT = -(Z0_LOW - 6e-3)
ALLSEL, ALL
VGEN, 1, ALL, , , X_SHIFT, 0, Z_SHIFT, , , 1
NUMCMP, VOLU
*get, vsteel, VOLU,, NUM, MAX
ALLSEL, ALL

! ===== air domain (graded): WP core R450um + inner R7 + mid R25 spheres + long outer R80xH70 cylinder =====
CORE_R     = 0.45e-3     ! [ADDED] WP fine core (< tip 500um -> pure air) for near-WP fit density
CORE_ESIZE = 0.022e-3    ! [MODIFIED] core elem size (0.022mm: R<=150um ~1400 pts; target >=1000)
SPH_FINE_R = 7e-3
MID_SPH_R  = 25e-3       ! [ADDED] mid air shell (7..25mm); single pole has NO yoke -> R25 clean
SPH_OFST = 0             ! [MODIFIED] WP now at ORIGIN after +x re-orientation
AIR_CYL_R = 80e-3
AIR_CYL_H = 70e-3
WPLANE, 1,0,0,0,,,,,,
WPcsys,-1,0
WPOFFS,0,0,SPH_OFST
SPHERE, 0, CORE_R, 0,360
SPHERE, 0, SPH_FINE_R, 0,360
SPHERE, 0, MID_SPH_R, 0,360
WPOFFS,0,0, SPH_OFST - AIR_CYL_H/2
CYL4,0,0,AIR_CYL_R, ,,,AIR_CYL_H

ALLSEL,ALL
VOVLAP,ALL
NUMCMP,VOLU

! ===== materials + element types =====
MAT_AIR1     = 1   ! inner air shell (CORE_R..7mm)
MAT_MT       = 2   ! 1018 steel (pole + support + protrusion)
MAT_AIR2     = 3   ! outer air cylinder
MAT_AIR_MID  = 4   ! [ADDED] mid air shell (7..25mm)
MAT_AIR_CORE = 5   ! [ADDED] WP fine core (R<450um), forced 0.1mm
mp , murx , MAT_AIR1     , 1
mp , murx , MAT_MT       , 280
mp , murx , MAT_AIR2     , 1
mp , murx , MAT_AIR_MID  , 1
mp , murx , MAT_AIR_CORE , 1
MAT_AIR_SLAB = 6   ! [ADDED-GAP] 200um base gap slab = air
mp , murx , MAT_AIR_SLAB , 1
ET_nocurr = 1
ET_curr   = 2
ET , ET_nocurr , SOLID96
ET , ET_curr   , SOURC36

! ===== [ADDED] VATT by location+size (VOVLAP cleared attrs -> classify, not hardcode) =====
!   1) vsz>1e-4   -> outer cylinder air MAT_AIR2 (~1.4e-3)
!   2) dwp>10mm   -> iron pole-body/support/protrusion MAT_MT (centroid radius ~47mm)
!   3) vsz>5e-7   -> inner air sphere MAT_AIR1 (~1.4e-6, centroid @ WP dwp~0)
!   4) else       -> iron tip segment inside sphere MAT_MT (tiny vsz, dwp<10mm)
ALLSEL,ALL
*get, VMAX, VOLU,, NUM, MAX
*do, vi, 1, VMAX
	VSEL,S,VOLU,,vi
	VSUM
	*get, vsz, VOLU, vi, VOLU
	*if, vsz, GT, 0, then
		*get, cx, VOLU, vi, CENT, X
		*get, cy, VOLU, vi, CENT, Y
		*get, cz, VOLU, vi, CENT, Z
		dwp = sqrt( cx*cx + cy*cy + (cz-SPH_OFST)*(cz-SPH_OFST) )
		*if, vsz, GT, 5e-4, then
			VATT, MAT_AIR2, 1, ET_nocurr       ! outer cylinder air (~1e-3)
		*elseif, dwp, GT, 10e-3, then
			VATT, MAT_MT, 1, ET_nocurr         ! steel support/pole-body (dwp>10mm)
		*elseif, vsz, GT, 1e-5, then
			VATT, MAT_AIR_MID, 1, ET_nocurr    ! mid air shell 7..25mm (~6e-5, dwp~0)
		*elseif, vsz, GT, 5e-7, then
			VATT, MAT_AIR1, 1, ET_nocurr       ! inner air shell CORE_R..7mm (~1.4e-6)
		*elseif, dwp, LT, 1e-3, then
			VATT, MAT_AIR_CORE, 1, ET_nocurr   ! [ADDED] WP core R<450um (dwp~0, vsz~4e-10)
		*else
			VATT, MAT_MT, 1, ET_nocurr         ! steel tip segment in inner sphere (dwp~3-5mm)
		*endif
	*endif
*enddo
ALLSEL,ALL

! [ADDED-GAP] slab -> MAT_AIR_SLAB (VATT loop 依 dwp>10 判成 steel，這裡覆蓋回 air)
!   slab 平移後: x[GAP_X+X_SHIFT .. +0.2e-3], z[-17e-3+Z_SHIFT .. -7e-3+Z_SHIFT], y±5
ALLSEL, ALL
VSEL, S, LOC, X, GAP_X+X_SHIFT, GAP_X+0.2e-3+X_SHIFT
VSEL, R, LOC, Z, -17e-3+Z_SHIFT, -7e-3+Z_SHIFT
VSEL, R, LOC, Y, -11e-3, 11e-3
VATT, MAT_AIR_SLAB, 1, ET_nocurr
ALLSEL, ALL
VSEL, S, MAT, , MAT_AIR_SLAB $ *GET, nv_slab, VOLU, 0, COUNT $ ALLSEL, ALL
/com, [ADDED-GAP] slab volumes assigned to air (MAT 6)

! [DIAG] per-volume vsz/dwp/mat (first-run classification check; delete once verified)
/com, ======= SINGLEPOLE VOLUME CLASSIFICATION DIAG (vi / vsz / dwp / mat) =======
*do, vi, 1, VMAX
	VSEL,S,VOLU,,vi
	VSUM
	*get, vsz, VOLU, vi, VOLU
	*if, vsz, GT, 0, then
		*get, cx, VOLU, vi, CENT, X
		*get, cy, VOLU, vi, CENT, Y
		*get, cz, VOLU, vi, CENT, Z
		dwp = sqrt( cx*cx + cy*cy + (cz-SPH_OFST)*(cz-SPH_OFST) )
		*get, vm, VOLU, vi, ATTR, MAT
		*VWRITE, vi, vsz, dwp, vm
('DIAG VOL ',F6.0,'   vsz=',E13.5,'   dwp[mm]=',F10.4,'   mat=',F4.0)
	*endif
*enddo
/com, ============================================================================
ALLSEL,ALL

! ===== SOURC36 coil (single lower coil @ 0deg; placeholder current, solve deck sets 1A) =====
COIL_OUT_R = 8e-3
COIL_IN_R  = 5e-3
TURNS = 70
COIL_THK = COIL_OUT_R - COIL_IN_R
COIL_R = (COIL_IN_R + COIL_OUT_R)/2
COIL_H = PROT_H
R,1,1,-TURNS*1,COIL_THK,COIL_H       ! [MODIFIED] flipped current direction; placeholder, solve deck sets sign+1A
WPLANE, 1,0,0,0,,,,,,
WPcsys,-1,0
NUMCMP,NODE
*get, NODE_NUM,NODE,,NUM,MAX
NODE_START = NODE_NUM + 1
X0_LOW = YOKE_MID_R*cos(0)
Y0_LOW = YOKE_MID_R*sin(0)
Z0_LOW = -PROT_H
! [MODIFIED] coil follows the +x re-oriented protrusion (add X_SHIFT/Z_SHIFT)
N , NODE_START   , X0_LOW + COIL_R + X_SHIFT , Y0_LOW          , Z0_LOW + COIL_H/2 + Z_SHIFT
N , NODE_START+1 , X0_LOW + X_SHIFT          , Y0_LOW + COIL_R , Z0_LOW + COIL_H/2 + Z_SHIFT
N , NODE_START+2 , X0_LOW + X_SHIFT          , Y0_LOW          , Z0_LOW + COIL_H/2 + Z_SHIFT
TYPE,ET_curr
REAL,1
E, NODE_START, NODE_START+1, NODE_START+2
/ESHAPE,1

! ===== mesh: WP core forced CORE_ESIZE + PURE SMRTSIZE,5 rest + EREFINE pole cone =====
!   核心球 forced CORE_ESIZE (填 WP 中心疏洞給 fit)；其餘 smrt5 (far-field 自適應)；極錐 EREFINE。
!   核心純空氣(tip 500um>450um)、界面 smrt5 conform 到 core 面 -> 平滑過渡、無 sliver。
LOCAL, 98, 2, 0, 0, SPH_OFST        ! spherical CS @ WP for radial cone selection
MSHAPE,1,3D
MSHKEY,0
! (1) WP 核心球 forced CORE_ESIZE
ESIZE, CORE_ESIZE
VSEL, S, MAT, , MAT_AIR_CORE
VMESH, ALL
ALLSEL, ALL
ESIZE, 0                            ! 清 forced size，讓 smrt5 接管
! (2) 其餘 (鋼+內殼+中殼+外圓柱) smrt5
SMRTSIZE, 5
VSEL, ALL
VSEL, U, MAT, , MAT_AIR_CORE
VMESH, ALL
ALLSEL, ALL

! Step1 pole cone +1 level (cone band R 2..18mm from WP; exclude tip R<2 + support/prot R>18)
ESEL,S,MAT,,MAT_MT
CSYS,98
ESEL,R,CENT,X, 2e-3, 18e-3
CSYS,0
*GET, nc1, ELEM, 0, COUNT
*IF, nc1, GT, 0, THEN
  EREFINE,ALL, , ,1, ,OFF
*ENDIF
ALLSEL,ALL
! Step2 pole cone +1 more level (single pole = lower pole -> 2 levels total)
ESEL,S,MAT,,MAT_MT
CSYS,98
ESEL,R,CENT,X, 2e-3, 18e-3
CSYS,0
*GET, nc2, ELEM, 0, COUNT
*IF, nc2, GT, 0, THEN
  EREFINE,ALL, , ,1, ,OFF
*ENDIF
ALLSEL,ALL
CSYS,0
/GO
*MSG, INFO, nc1, nc2
 EREFINE pole cone: L1=%G L2=%G elems

! ===== [MODIFIED] MESH-ONLY: report + SAVE db for GUI inspection (no BC, no /SOLU) =====
!   solve deck later RESUMEs this .db + adds D,ALL,MAG,0 + CURR + magsolv (mesh/solve separated).
ALLSEL, ALL
NUMCMP, NODE
NUMCMP, ELEM
*GET, nnode, NODE, 0, COUNT
*GET, nelem, ELEM, 0, COUNT
/GO
*MSG, INFO, nnode, nelem
 MESH DONE (singlepole filled +x): %G nodes / %G elems
ESEL, S, MAT, , MAT_AIR_SLAB $ *GET, ne_slab, ELEM, 0, COUNT $ ALLSEL, ALL
*MSG, INFO, ne_slab
 GAP slab air elems = %G
SAVE
FINISH
