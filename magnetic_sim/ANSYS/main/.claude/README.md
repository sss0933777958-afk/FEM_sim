# .claude/ — Claude Code 本地設定

此資料夾是 Claude Code 的**本地設定**，非工作 / 模擬產物。

- `settings.local.json` — 本機權限 allowlist 等（gitignored，見 repo `.gitignore`）。

## 規則
- 通常不需手動編輯；要改設定用 `/config` 或對應指令。
- 全域工作規則在上層 `../CLAUDE.md`；path-scoped 規則在 repo `../../../../.claude/rules/`。
