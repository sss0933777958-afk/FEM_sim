# long2016_hexapole_halfcut/field_viz/code — 程式碼層

**用途**：field_viz 功能組的程式碼容器。純繪圖組，底下只有 `plot/`。

**內容**：`plot/`（所有場視覺化腳本，見 `plot/README.md`）。

**資料來源 / 流向**：腳本讀 ANSYS_data `.dat`（`ansys_path`）→ 畫真實節點 → 出 PNG 到 `../figures/`。

**命名 / 慣例**：純繪圖功能組只有 `code/plot/`（無 `scripts/` / `main/`）。

**相關**：見 `../README.md`（功能組說明）、`../../../CLAUDE.md`（繪圖規則）。
