# long2016_hexapole_halfcut/sensor_placement/figures — sensor 擺放定案輸出圖

**用途**：放 sensor_placement 已定案的輸出 PNG。

**內容**：`Bsurf_placement_P1..P6.png`、`Bsurf_disc_sampling.png`、`upper/lower_pole_sensor_placement*.png`（含 1p572 / 4p472 變體）、`P1/P2_geom_vectors_nhat_ahat.png`、`btip_sensor_placement.png`。

**資料來源 / 流向**：由 `../code/plot/*.m` 依幾何 / FEM 場輸出至此。

**命名 / 慣例**：圖檔名對應繪圖腳本；定案後才存最終圖；真實節點 / 幾何（非內插）。

**相關**：見 `../README.md`、`../../../CLAUDE.md`。
