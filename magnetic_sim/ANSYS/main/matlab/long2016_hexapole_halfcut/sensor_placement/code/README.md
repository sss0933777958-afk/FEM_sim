# long2016_hexapole_halfcut/sensor_placement/code — 程式碼層

**用途**：sensor_placement 功能組的程式碼容器。純繪圖組，底下只有 `plot/`。

**內容**：`plot/`（sensor 幾何 / 位置 / 靈敏度繪圖腳本，見 `plot/README.md`）。

**資料來源 / 流向**：依幾何常數（`mt_constants`）+ 視需要 ANSYS_data 場 → 畫真實節點 / 幾何 → PNG 到 `../figures/`。

**命名 / 慣例**：純繪圖功能組只有 `code/plot/`。

**相關**：見 `../README.md`、`../../../CLAUDE.md`。
