# long2016_hexapole_halfcut/bs_matrix/code/ — bs_matrix 程式碼層

**用途**：bs_matrix 功能組的程式碼容器（此組為純計算 + LaTeX 產生，無繪圖）。

**內容**：
- `scripts/` — 6×6 轉移矩陣計算與 LaTeX 產生腳本。

**資料來源 / 流向**：見上層 `../README.md`（讀 `ANSYS_data/coilN` FEM `.dat` → 算 → `MATLAB_data/<model>/bs_matrix/*.mat` + `results/*.tex`）。

**命名 / 慣例**：`code/scripts` schema（無 `plot/`）。

**相關**：見上層 `../README.md`、`../../common/README.md`、`../../../../CLAUDE.md`。
