# long2016_hexapole_halfcut/sensor_d/code/ — sensor_d 程式碼層

**用途**：sensor_d 功能組的程式碼容器（純計算 + LaTeX 產生，無繪圖）。

**內容**：
- `scripts/` — Hall-sensor 常數 `d` 的求解 / 驗證 + LaTeX 產生腳本。

**資料來源 / 流向**：見上層 `../README.md`（讀 `MATLAB_data/<model>/charge_fit/calib_sensor_d.mat` → 算 → `doc/charge_model_fitting/.../scripts/*.tex`）。

**命名 / 慣例**：`code/scripts` schema（無 `plot/`）。

**相關**：見上層 `../README.md`、`../../common/README.md`、`../../../../CLAUDE.md`。
